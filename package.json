{
  "name": "doom-dagger-pipeline",
  "version": "0.1.0",
  "description": "Dagger pipeline for testing Doom Emacs configuration",
  "type": "module",
  "scripts": {
    "pipeline": "node --loader ts-node/esm .dagger/config.ts"
  },
  "dependencies": {
    "@dagger.io/dagger": "^0.18.0",
    "morphcloud": "^0.0.9"
  },
  "devDependencies": {
    "ts-node": "^10.9.2",
    "typescript": "^5"
  },
  "engines": {
    "node": ">=14"
  }
}
